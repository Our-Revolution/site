language: python
python:
  - "2.7"
# safelist
branches:
  only:
  - master
# install dependencies
install:
  - npm install
  - easy_install distribute
  - pip install -r requirements.txt
# run tests
script:
  - echo skip tests
